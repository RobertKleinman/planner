# ============================================================
# Planner Configuration
# ============================================================
# Copy to .env and fill in your values. NEVER commit .env to git.
# ============================================================

# --- AI Services ---
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# --- Your API Key (phone → server authentication) ---
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
PLANNER_API_KEY=change-me-to-something-random

# --- Database ---
DATABASE_URL=sqlite:///./planner.db

# --- Google APIs ---
# These are populated automatically by setup_google.py after OAuth flow.
# You'll need to create a Google Cloud project first (see README).
GOOGLE_CREDENTIALS_FILE=credentials.json
GOOGLE_TOKEN_FILE=token.json

# --- Twilio (SMS to Johnny) ---
# Get yours at: https://console.twilio.com
TWILIO_ACCOUNT_SID=your-twilio-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890
JOHNNY_PHONE_NUMBER=+1234567890

# --- Email (Daily Digest) ---
# Uses Gmail API via the same Google OAuth credentials above.
DIGEST_RECIPIENT_EMAIL=your-email@gmail.com

# --- Server ---
ENVIRONMENT=development
TIMEZONE=America/Toronto
